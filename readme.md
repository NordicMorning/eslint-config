# eslint-config-nordic-morning

## Usage

`npm i @nordic-morning-se/eslint-config -D`

Then add a `.eslintrc` file to your project that contains:

```
{
  "extends": "@nordic-morning-se/eslint-config"
}
```